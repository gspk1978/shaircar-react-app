{
  "name": "shaircar-react-app",
  "version": "1.0.0",
  "scripts": {
    "start-client": "cd client && npm start",
    "server": "node server.js",
    "build": "npm install && (cd client && npm run build)",
    "installboth": "npm i && cd client && npm i",
    "startboth": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "concurrently": "^6.0.0",
    "express": "^4.17.1",
    "request": "^2.88.2"
  }
}
